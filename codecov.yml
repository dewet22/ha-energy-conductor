coverage:
  status:
    project:
      default:
        target: 90%      # absolute floor — matches pytest fail_under
        threshold: 2%    # allow up to 2 % drop between commits
    patch:
      default:
        target: 90%
